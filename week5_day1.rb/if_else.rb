#make a new ruby program (make a new file)
#Asks someone for a number
#Then create a condition that sees if that number is 
#greater than, b) equal to

puts "Give me a number."

number_string = gets.chomp
number = number_string.to_i

puts "Now enter a password"
password = get.chomp

#Look for a password of correct to give a message if the number 
#does not equal 15 

if number  < 15 && password == "correct"
	puts "That number is less than 15"
elsif number == 15
	puts "I can't tell you anything because you don't know the right password"
elsif number > && password "correct"
	puts "That number is greater than 15"  
else "I can't tell you anything because you don't have the right password"
end
"Secret number" is 15


 number = get.chomp

 if number < 15
 	puts "That number is less than 15"
 elsif number 
 	

 	#conditional
 	"apple" "pear" "oranges" "grapefruit" "lemon"

 	#if fave fruit is apple or pear, tell them one thing.
 	#if fave fruit is o, g, ,l, tell them something else. 
 	#if its none of the things, tell them you don't understand what there saying.

puts "What is your favorite of the five fruits listed?"

fave_fruit = get.chomp

if fruit == "pear" || fruit == "apple"
	puts "Yum. That's good fruit"
elsif fruit == "orange" || fruit == "grapefruit" || fruit == "Lemon"
	puts "Looks like you like citrus!"
else
	"Hmm...I've never heard of that one"
end

var myArray = ["element", "elemnt2", "element3"];
console.log(element)
for (i = 0 < myArray.length; i ++) {
	console.log(myArray[i]);
}


i = 0
while(i < myArray.length {
	console.log(myArray[i])
	i++
	})



#for loop
my_array = ["element1","elemnt2", "element3"]
0..4 == (0,1,2,3,4)
for element in my_array
	puts "#{element}"
end


(0..4).each do |i|
	puts 
end

i = 0 
while i < 10
	puts "This number is less than 10!"
	i++
end

function subtractTwo(n)
	#the last line of a function is its return value!
	n- 2
end
	#If either arg is 0, we don't want to do any math on those numbers. We want 
	#to return out ot the function early.
def divide(x,y)
	if x == 0|| y == 0 
		return "We don't do math on zeros"

	x/y

	end


#Exercises

# create the following methods:
-# adds five to arguement given
-# multiplies arguments given by 5
-# performs a mathmatical operation using four intergers and/oranges float arguments
-# prints the argument given four times
-# prints an uppercase version of the argument given


def  add_five(n)
 n + 5 
end

def multiply_fifteern(n)
	n*15
end

def secret math(a, b, c, d)
	a + b -c * d
		
	end

	def print_four(word)
		for i in 1..4
			puts word
		end
	end

def uppercase (n)

end



#upcase
#downcase
#titleize
#captitalize





