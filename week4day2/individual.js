//Write a function named stringy that takes a size 
//and  a string of alternating ‘1s’ and ‘0s’. 
//the string should start with a 1. 
//a string with size 6 should return :’101010’. 
//with size 4 should return : ‘1010’. 
//with size 12 should return : ‘101010101010’. 
//The size will always be positive and will only use whole numbers.
function stringy (size) {
	var result = "";

    var ten = "10"
	var times = size /2

	var negative = size <= 0

	k
	}
	if (size % 1 !=)

	for (i=1; i<=times; i++){
		result += "10"

	} if (size %)



	
	 
}

var stringy = [ 1,];

for (var i=0; i < stringy.length; i ++) {
	alert(myStringy[i]+"1");
	alert()
}
alert(hi mandy
	)

//coupons




funcitons checkcoupon(code, expirationDate) {
	checkCode 

	//if valid
	return true;
	//if invalid
	return false;
}

function checkCode (code) {
	if (code.lenth != 6) {
		return false;
	}
}
 var codeArray = code.split("")
 var letters = [];
 var numbers = [];

 codeArray.forEach(function(element) {
 	if (isNaN(element)){
 		letters.push(element);
 	} else {
 		numbers.push(element);
 	}
 }
 	if(letters.lenght == 3 && numbers.length == 3) {
 		return = true;
 		} else {
 			return = false;
 		}
 	}

 	function isValidExpiration(expirationDate);

 	var today = new Date;
 	var expiration = new Date (expirationDate);
 	var expDay = expiration.getDate()
 	expiration.setDate(expDay +1)

 	return today > expiration;

 		retun true;
 	} else {
 		return false;

 	}
